<% provide(:title, 'Discuss') %>
<h1>
	All Users
</h1>

<div class = "users">
	<ul>
		<%  users = User.select(:id,:name) %>
		<% users.each do |user| %>
			<li>
				<%= link_to user['name'] , user_path(user) %>
			</li>
		<% end %>
	</ul>

</div>